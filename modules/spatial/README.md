# spatial

当前交付范围是 AABB 插入、更新、删除与交叠批量查询。真实 rstar 0.12.2 默认后端与独立暴力参考后端共享接口，不共享实现。第三方类型不出 Adapter。

所有实际入口验证有限坐标和有序边界；默认对象、查询数和结果数有上限。容量先检查，输出失败不部分覆盖。SpatialResource 提供生命周期围栏和实际存储回收。

配置和消费见 [Spatial 后端](../../docs/specs/spatial-backend.md)。不把尚未实现的完整 BVH、距离、邻域、连续碰撞算法计为完成；性能需真实数据分布基线。
